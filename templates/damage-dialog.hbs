<form class="vaesen-dmg-dialog">
  <div class="form-group">
    <label>Danno base (da arma/incantesimo)</label>
    <input type="number" name="baseDamage" value="{{baseDamage}}" min="0" />
  </div>

  <div class="form-group">
    <label>Successi extra convertiti in danno (+1 ciascuno)</label>
    <input type="number" name="extraToDamage" value="{{extraToDamage}}" min="0" />
    <p class="notes">Se preferisci usare gli extra per “stunt”, lascia 0.</p>
  </div>

  <div class="form-group">
    <label class="checkbox">
      <input type="checkbox" name="isMental" {{#if isMental}}checked{{/if}}/>
      Danno mentale (usa path mentale)
    </label>
  </div>

  <p class="hint">Bersagli: usa i token attualmente selezionati sul canvas.</p>
</form>
